version: "3.1"
services:
    frontend:
        container_name: frontend
        build: .
        image: frontend:latest
        ports: 
            - "8080:8080"

networks:
    default:
        external:
            name: eventmod-network